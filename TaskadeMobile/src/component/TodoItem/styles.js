import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    subContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        marginVertical: 3,
    },
    textInput: {
        width: '82%',
        fontSize: 14,
        color: 'white',
        marginLeft: 10,
        marginRight: 10,
        borderColor: '#bfbfbf',
        borderWidth: 1,
        borderRadius: 20,
    },
});

export default styles;
