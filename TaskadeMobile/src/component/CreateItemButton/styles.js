import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    createNewItem: {
        marginTop: 12,
        marginBottom: 12,
        backgroundColor: '#739019',
        borderRadius: 10,
        alignSelf: 'center',
    },
    createContainer: {
        flexDirection: 'row',
        padding: 7,
    },
    newItemText: {
        color: '#fff',
        fontSize: 16,
        marginLeft: 5,
    },
});

export default styles;
